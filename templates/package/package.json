{
  "name": "@livedesigner/REPLACEME",
  "main": "lib/index.js",
  "version": "VERSION",
  "description": "TODO: add description.",
  "author": "The Haiku Team <contact@haiku.ai>",
  "license": "Proprietary",
  "publishConfig": {
    "registry": "https://registry.yarnpkg.com"
  },
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "health": "jest --ci -c jest.ci.js && tslint -p . -t checkstyle -o checkstyle-result.xml",
    "lint": "tslint -p . -t stylish",
    "fix": "tslint -p . -t stylish --fix"
  },
  "dependencies": {
    "@livedesigner/engine": "VERSION"
  },
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "istanbul-reporter-cobertura-haiku": "^1.0.4",
    "jest": "^23.6.0",
    "jest-tap-reporter": "^1.9.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.15.0",
    "tslint-config-haiku": "^1.0.18",
    "typescript": "^3.4.1"
  }
}
