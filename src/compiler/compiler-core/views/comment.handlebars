{{#ifIsCommentable .}}
/**
{{#with description.body}}
{{{indent . ' * '}}}
 *
{{/with}}
{{#if presentation.value}}
  {{#if presentation.reference}}
 * `{{{presentation.reference}}}` ( {{{presentation.value}}} )
  {{else}}
 {{~{indent presentation.value ' * ' ' | '}}}
  {{/if}}
{{else}}
  {{#if presentation.reference}}
 * `{{{presentation.reference }}}`
  {{/if}}
  {{#each presentation.properties}}
 * - {{@key}}: `{{{.}}}`
  {{/each}}
{{/if}}
 */
{{/ifIsCommentable}}
