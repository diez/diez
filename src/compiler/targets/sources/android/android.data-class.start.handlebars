{{#if hasProperties}}
data class {{{type}}}(
{{#if fixed}}
{{#each properties}}
    val {{{this.name}}}: {{{this.type}}} = {{{this.initializer}}}{{#unless @last}},{{/unless}}
{{/each}}
{{else}}
{{#each properties}}
    val {{{this.name}}}: {{{this.type}}}{{#unless @last}},{{/unless}}
{{/each}}
{{/if}}
{{else}}
data class {{{type}}}(
    val nonce: String = ""
{{/if}}
{{#if isRootComponent}}) : RootComponent {
    companion object {}
    override val name = "{{{type}}}"
{{else}}
) {
    companion object {}
{{/if}}
