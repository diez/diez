{{#if isRootComponent}}
export declare class {{{type}}} extends RootComponent {
{{else}}
export declare class {{{type}}} {
{{/if}}
  {{#each properties}}
  {{{this.name}}}: {{this.type}};
  {{/each}}
}
