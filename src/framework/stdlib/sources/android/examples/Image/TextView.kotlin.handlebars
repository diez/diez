import org.diez.{{case projectName style="camel"}}.*

val designLanguage = {{case rootType style="pascal"}}()
val image = designLanguage.{{path}}

val textView: TextView = // Get a reference to a TextView.
textView.loadLeftDrawable(image)
