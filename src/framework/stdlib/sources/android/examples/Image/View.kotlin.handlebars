import org.diez.{{case projectName style="pascal"}}.*

val designSystem = {{case rootType style="pascal"}}()
val image = designSystem.{{path}}

val view: View = // Get a reference to a View.
view.loadBackgrounImage(image)
