import org.diez.{{case projectName style="camel"}}.*

val designLanguage = {{case rootType style="pascal"}}()
val linearGradient = designLanguage.{{path}}

val drawable = PaintDrawable()
drawable.shape = RectShape()
drawable.shaderFactory = linearGradient.shaderFactory

val view: View = // Get a reference to a View.
view.background = drawable
