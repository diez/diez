import org.diez.{{case projectName style="pascal"}}.*

val designSystem = {{case rootType style="pascal"}}()
val lottie = designSystem.{{path}}

val animationView: LottieAnimationView = // Get a reference to a LottieAnimationView.
animationView.load(lottie)
