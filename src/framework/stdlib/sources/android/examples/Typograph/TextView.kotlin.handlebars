import org.diez.{{case projectName style="pascal"}}.*

val designSystem = {{case rootType style="pascal"}}()
val typograph = designSystem.{{path}}

val textView: TextView = // Get a reference to an TextView.
textView.apply(typograph)
