import Diez{{case projectName style="pascal"}}

let designLanguage = {{case rootType style="pascal"}}()
let linearGradient = designLanguage.{{path}}

let view = LinearGradientView()
view.apply(linearGradient)
