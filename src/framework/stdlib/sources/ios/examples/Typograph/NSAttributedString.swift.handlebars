import Diez{{case projectName style="pascal"}}

let designLanguage = {{case rootType style="pascal"}}()
let typograph = designLanguage.{{path}}

let attributedString = NSAttributedString(string: "Ten", typograph: typograph)
