{
  "name": "@livedesigner/engine",
  "main": "lib/index.js",
  "version": "1.0.0-alpha0",
  "description": "The Diez Engine.",
  "author": "The Haiku Team <contact@haiku.ai>",
  "license": "Proprietary",
  "publishConfig": {
    "registry": "https://registry.yarnpkg.com"
  },
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "health": "jest --ci -c jest.ci.js && tslint -p . -t checkstyle -o checkstyle-result.xml",
    "lint": "tslint -p . -t stylish",
    "fix": "tslint -p . -t stylish --fix"
  },
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "istanbul-reporter-cobertura-haiku": "^1.0.4",
    "jest": "^23.6.0",
    "jest-tap-reporter": "^1.9.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-config-haiku": "^1.0.18",
    "typescript": "^3.2.4"
  }
}
