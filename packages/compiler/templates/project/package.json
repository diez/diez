{
  "name": "{{packageName}}",
  "main": "lib/index.js",
  "version": "1.0.0",
  "dependencies": {
    "@livedesigner/engine": "{{diezVersion}}",
    "@livedesigner/designsystem": "{{diezVersion}}"
  },
  "devDependencies": {
    "@livedesigner/compiler": "{{diezVersion}}",
    "@livedesigner/targets": "{{diezVersion}}",
    "typescript": "{{typescriptVersion}}"
  }
}
